# example-post

Drop one or more media files into `./media/` and reference them from `post.yaml`,
then run:

```bash
auto-li publish ./content/example-post --account demo --dry-run
```

Remove `--dry-run` when you're confident.
